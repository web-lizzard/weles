import type { JSX, ReactNode } from "react";

type BottomPanelProps = {
  hints: string;
  children: ReactNode;
};

export default function BottomPanel(_props: BottomPanelProps): JSX.Element {
  throw new Error("not implemented");
}
