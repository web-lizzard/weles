import type { JSX } from "react";

type StatusLineProps = {
  columns: number;
  showApproveHint: boolean;
};

export default function StatusLine(_props: StatusLineProps): JSX.Element {
  throw new Error("not implemented");
}
