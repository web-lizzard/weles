import type { JSX } from "react";

type ActivityIndicatorProps = {
  startedAt: number;
};

export default function ActivityIndicator(
  _props: ActivityIndicatorProps,
): JSX.Element {
  throw new Error("not implemented");
}
